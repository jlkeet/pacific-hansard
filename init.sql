CREATE TABLE documents (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255),
    date DATE,
    speaker VARCHAR(255),
    speaker2 VARCHAR(255),
    document_VARCHAR(255),
    content TEXT
);
